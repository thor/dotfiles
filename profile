#!/bin/zsh

# This profile may be sourced by many a few.
# LightDM sources this upon login, as do others that use Xprofile.
# I prefer this to be ~/.profile, however.

[ -f "$HOME/.zshenv" ] && . "$HOME/.zshenv"

